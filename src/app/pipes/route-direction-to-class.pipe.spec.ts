import { RouteDirectionToClassPipe } from './route-direction-to-class.pipe';

describe('RouteDirectionToClassPipe', () => {
  it('create an instance', () => {
    const pipe = new RouteDirectionToClassPipe();
    expect(pipe).toBeTruthy();
  });
});
